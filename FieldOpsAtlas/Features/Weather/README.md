# Weather

Weather uses the shared FieldOps shell and a filtered OSM Leaflet base map.

Provider scripts stay separate:

- `rainviewer.js`
- `openmeteo.js`
- `metoffice.js`
- `ea-rainfall.js`

The OSM base pane is colour-filtered so provider overlays keep the Met Office rainfall palette visually distinct.
